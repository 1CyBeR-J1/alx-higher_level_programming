#!/usr/bin/python3
def weight_average(my_list=[]):
    if not my_list:
        return 0
    my_list = (<score>, <weight>)
    a = sum(map(lambda x: x[0] * x[1], mylist))
    b = sum(map(lambda x[1], my_list))
    return a/b
